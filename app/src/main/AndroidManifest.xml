<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.aniruddhfichadia.presentableexample">

    <application
        android:name=".DemoApplication"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:theme="@style/AppTheme">

        <activity
            android:name=".LauncherActivity"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>

        <activity
            android:name=".demo.partialrestoration.PartialRestorationDemoActivity"
            android:label="Partial restoration"/>

        <activity
            android:name=".demo.fullrestoration.FullRestorationDemoActivity"
            android:label="Full restoration"/>

    </application>

</manifest>